# リポジトリ運用規約

## 目的

このリポジトリでは、ASD ESDの次期サイトを構築・保守する。公開サイトはAstroで作成し、コンテンツはmicroCMSで管理し、Cloudflare Pagesで静的ファイルを公開する。

## 現在の状態

- このリポジトリはゼロから再構築中である。
- 現行サイトは内容を確認したが、文言・画像・製品情報の移行には公開承認が必要である。
- 要件、ビジュアル方針、最終コンテンツ、microCMSの認証情報は未確定である。
- 会社情報、価格、連絡先、法的表現を推測で作成してはならない。

## 採用予定の技術

- AstroとTypeScript
- 明確な利点がある場合のみTailwind CSS
- 編集可能なコンテンツのためのmicroCMS
- 静的ホスティングのためのCloudflare Pages
- ソース管理とデプロイ連携のためのGitHub

## アーキテクチャ制約

- すべての公開ページを静的HTMLとして生成する。
- microCMSのコンテンツ取得はビルド時だけに限定する。
- microCMSのAPIキーをブラウザへ公開してはならない。
- 明示的な承認なしにSSR、Cloudflare Workers、Pages Functions、データベース、独自認証、独自管理画面を追加してはならない。
- 明示的な承認なしに有料または従量課金サービスを追加してはならない。
- 明示的な依頼なしに請求設定、支払い方法、デプロイ、外部アカウント設定を変更してはならない。

## ドキュメント

- 実装判断の前に、`docs/` 内の関連文書を確認する。
- アーキテクチャ、コンテンツ項目、URL、運用手順を変更した場合は、関連文書も更新する。
- 重要な判断は`docs/decisions.md`に記録する。

## コード注釈規約

- HTML以外のプログラミング行には、同じ行または直前の行に日本語の注釈を最低1つ付ける。
- 関連するコードを変更したら、日本語注釈も正確な内容に更新する。
- この規約はTypeScript、JavaScript、CSS、設定、ワークフロー、スクリプトに適用する。
- Markdownには、読者にとって有用な場合を除き注釈を追加しない。

## 実装基準

- TypeScriptを導入する場合はstrict modeを使用する。
- Astroコンポーネント、セマンティックHTML、最小限のクライアント側JavaScriptを優先する。
- 公開テキストは中国語と英語と日本語を基本とする。
- アクセシブルなラベル、キーボード操作、レスポンシブレイアウトを実装する。
- 外部入力は検証し、安全に失敗させる。
- 秘密情報、APIキー、非公開URL、本番データのエクスポートをコミットしてはならない。

## 検証

- 利用者に見える挙動を変える場合は、必要に応じてテストを追加または更新する。
- 完了報告前に、関連するlint、型検査、ビルド、ブラウザテストを実行する。
- 実行した検査、失敗した検査、実行できなかった検査を明確に報告する。

## エージェントの作業手順

1. 編集前に関連コードと文書を確認する。
2. 重要な前提条件に依存する前に、その前提を明示する。
3. 変更範囲を依頼内容に限定する。
4. 関係のない利用者変更を保持する。
5. 最終差分をセキュリティ、アクセシビリティ、コスト、回帰の観点で確認する。
6. 変更ファイル、検証結果、残るリスクを要約する。
