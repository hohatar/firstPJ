{
  "name": "asd-esd-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "astro": "^7.1.1",
    "wrangler": "^4.112.0"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.3"
  }
}
